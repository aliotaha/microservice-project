server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: eureka-server

management:
  metrics:
    export:
      zipkin:
        enabled: true
  tracing:
    zipkin:
      enabled: true
      baseUrl: http://localhost:9411/
